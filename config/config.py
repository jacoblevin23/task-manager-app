# This file contains the configuration settings for the application.
# It includes settings for the database connection and other application-specific configurations.

DB_CONFIG = {
    "host": "localhost",
    "user": "root",
    "password": "",
    "database": "task_db"
}
